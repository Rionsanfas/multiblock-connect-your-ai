# Environment Variables Example
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# Supabase Configuration (Required)
# These are PUBLIC/publishable keys - safe to expose in frontend
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# Note: The following secrets are configured in Supabase Edge Function secrets
# and are NOT stored in this file:
# - SUPABASE_SERVICE_ROLE_KEY (auto-provided by Supabase)
# - SUPABASE_URL (auto-provided by Supabase)
# - SUPABASE_ANON_KEY (auto-provided by Supabase)
# - POLAR_ACCESS_TOKEN (configured in Supabase secrets)
# - POLAR_WEBHOOK_SECRET (configured in Supabase secrets)
